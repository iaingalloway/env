# ~/.config/starship.toml

'$schema' = 'https://starship.rs/config-schema.json'

add_newline = true

format = """\
[](fg:#9A348E)\
$shell\
[](bg:#DA627D fg:#9A348E)\
$directory\
[](bg:#D98B6C fg:#DA627D)\
$git_branch\
$git_commit\
$git_state\
$git_metrics\
$git_status\
[](bg:#86BBD8 fg:#D98B6C)\
$docker_context\
[](bg:#06969A fg:#86BBD8)\
$kubernetes\
[](fg:#06969A)
$jobs\
$sudo\
$character"""

[character]
success_symbol = '[](bold green)'
error_symbol = '[](bold red)'

[directory]
format = '[ $path ]($style)'
style = 'bg:#DA627D'
home_symbol = ''
truncate_to_repo = true
truncation_symbol = '…/'

[git_branch]
format = '[ $branch]($style)'
style = 'bg:#D98B6C'
symbol = ''
truncation_length = 20
truncation_symbol = '…/'

[git_commit]
style = 'bg:#D98B6C'
commit_hash_length = 4
tag_symbol = '🔖 '

[git_metrics]
added_style = 'bold blue'
format = '[+$added]($added_style)/[-$deleted]($deleted_style) '

[git_state]
style = 'bg:#D98B6C'
format = '[\($state($progress_current of $progress_total)\)]($style) '
cherry_pick = '[🍒 PICKING](bold red)'

[git_status]
format = '[ $up_to_date$conflicted$untracked$modified$renamed$deleted$stashed$ahead_behind$staged ]($style)'
style = 'bg:#D98B6C'
conflicted = '🏳'
up_to_date = '[](bg:#D98B6C fg:#2B994A)'
untracked = '[](bg:#D98B6C fg:#B82E2E)'
ahead = '${count}'
diverged = '${ahead_count}${behind_count}'
behind = '${count}'
stashed = ''
modified = '[](bg:#D98B6C fg:#B82E2E)'
staged = '+$count'
renamed = '襁'
deleted = ''

[docker_context]
style = 'bg:#86BBD8'
only_with_files = false

[kubernetes]
style = 'bg:#06969A'
disabled = false
symbol = '⎈'
format = '[ $symbol$context( \($namespace\)) ]($style)'

[kubernetes.context_aliases]
'gke.*_(?P<var_cluster>[\w-]+)' = 'gke-$var_cluster'
'kind-(?P<var_cluster>[\w-]+)' = '$var_cluster'

[shell]
disabled = false
format = '[$indicator ]($style)'
style = 'bg:#9A348E'
bash_indicator = ''
powershell_indicator = ''
